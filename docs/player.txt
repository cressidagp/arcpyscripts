getName()
sendChatMessage( type, lang, msg )
toUnit()
spawnAndEnterVehicle( creatureId, delay )
teleport( mapId, x, y, z, o )
sendGossipPOI( x, y, icon, flags, data, name )
markQuestObjectiveAsComplete( quest, objective )
addQuestKill( quest, objective )
sendMovie( movieId )
sendCinematic( cinematicId )
addItem( itemId, ammount )
removeItem( itemId, ammount )
hasItem( itemId, ammount )
getSession()
startTaxi( taxiPathId, displayId, startNode )
hasQuest( questId )
sendAreaTriggerMessage( message )
broadcastMessage( message )
getSelectedUnit()